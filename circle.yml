---
machine:
    java:
        version: oraclejdk8

test:
    post:
        - ./gradlew javadoc
        - mkdir -p "$CIRCLE_TEST_REPORTS/junit"
        - find . -path '*/build/test-results/TEST-*.xml' -exec cp {} "$CIRCLE_TEST_REPORTS/junit" \;
