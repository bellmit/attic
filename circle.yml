---
machine:
    java:
        version: oraclejdk8

dependencies:
    post:
        - ./gradlew stage

test:
    override:
        - ./gradlew check
        - cp -a build/test-results "${CIRCLE_TEST_REPORTS}/junit"
