---
machine:
    java:
        version: oraclejdk8

checkout:
    post:
        - bin/use-pull-request-merge

dependencies:
    override:
        - ./gradlew classes
        - ./gradlew testClasses

test:
    post:
        - ./gradlew javadoc
        - bin/collate-test-results "$CIRCLE_TEST_REPORTS"
